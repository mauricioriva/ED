par :: Int -> String
par n = if (mod n 2) == 0 then " es par " else " no es par"

pitagoras :: Float -> Float -> Float
pitagoras x y = sqrt ((x**2) + (y**2))

incrementa :: Int -> Int
incrementa n = n + 1
